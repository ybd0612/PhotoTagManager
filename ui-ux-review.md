# PhotoTagManager UI/UX 与文案优化审查

日期：2026-08-20
范围：现有 React/MUI/Tailwind 页面、图片浏览、目录树、标签筛选、预览层；本轮只读审查，未修改业务代码。

## 结论

当前界面已经具备桌面图片管理器的基本结构：顶部工具栏、左侧根目录/目录树、右侧标签筛选与缩略图、底部扫描状态、全屏预览。下一阶段不建议先做大规模换肤，优先解决信息层级、操作反馈、键盘可达性和小窗口布局问题。

## P1：建议优先处理

### 1. 右侧图片区缺少稳定的上下文信息
- 位置：`src/components/AppLayout.tsx` 289-295、`src/components/ThumbnailGrid.tsx` 143-153
- 现状：右侧主要依靠标签条和空状态文字表达当前目录；有图片时没有清晰的“当前目录 / 当前结果数 / 筛选结果数”标题。
- 影响：用户切换目录后容易失去上下文，尤其是根目录下递归汇总图片时，不容易知道当前显示的是当前目录还是全部子目录。
- 建议：在标签筛选条上方增加轻量上下文栏，例如“照片 / 2024 / 1,248 张图片”，筛选后显示“显示 86 / 1,248 张”；不要重复展示完整绝对路径。

### 2. 顶部完整路径会挤压操作区
- 位置：`src/components/AppLayout.tsx` 195-203、206-237
- 现状：根别名和完整绝对路径都放在 Chip 中，最大宽度 420px；窗口较窄或路径很长时会压缩“添加根目录、重新扫描、检查更新”等操作。
- 建议：顶部只保留“根目录别名 · 当前相对目录”，完整路径放入 Tooltip/可复制详情；操作区保持不被路径挤压。

### 3. 左侧根目录操作依赖 hover
- 位置：`src/components/AppLayout.tsx` 435-455
- 现状：改名、删除按钮使用 `opacity-0 group-hover:opacity-100`，键盘焦点和触控场景下不明显。
- 影响：用户不知道根目录还有操作；键盘用户难以发现入口。
- 建议：改为一个始终可见但低对比度的“更多”按钮，菜单内放“修改别名、删除”；或至少补 `:focus-visible` 显示规则与 aria-label。

### 4. 扫描过程反馈不够完整
- 位置：`src/components/AppLayout.tsx` 299-329、`src/components/ThumbnailGrid.tsx` 147-153
- 现状：底部只显示扫描百分比和文件数；扫描失败只显示“扫描出错”，取消状态没有明确文案；空目录提示“图片会自动出现…”也没有说明当前是否仍在扫描。
- 建议：统一状态文案：
  - 扫描中：`正在扫描：已发现 1,240 个文件（42%）`
  - 已取消：`扫描已取消，当前显示已发现的图片`
  - 失败：`扫描失败：原因……` + `重试扫描`
  - 未扫描：`尚未扫描此目录` + `开始扫描`

### 5. 标签筛选 AND/OR 不够面向用户
- 位置：`src/components/TagFilterBar.tsx` 103-118
- 现状：直接展示英文逻辑词 AND / OR。
- 建议：显示“同时包含 / 任一包含”，Tooltip 或辅助说明中再保留 `AND / OR`；“热门”建议改为“常用标签”，更符合实际是按数量排序而不是时间热度。

### 6. 全局禁止文本选择过于宽泛
- 位置：`src/styles/index.css` 31-33
- 现状：`body { user-select: none; }`，导致路径、错误信息、标签等文本默认不可复制。
- 建议：删除全局禁止选择，只在缩略图卡片、预览拖拽画布等确实需要防止误选的区域设置 `user-select: none`；路径和错误信息允许复制。

## P2：第二阶段优化

### 7. 缩略图卡片信息不足
- 位置：`src/components/ThumbnailGrid.tsx` 168-213
- 现状：图片卡片基本只有图片、最多 3 个标签和选中图标；文件名主要依赖路径或预览。
- 建议：在卡片底部增加渐变遮罩和文件名（超长省略），标签数量超过 3 个时显示 `+N`；选中状态同时增加轻微底色/边框，不只依赖右上角图标。

### 8. 缩略图比例和卡片尺寸缺乏设置
- 位置：`src/components/ThumbnailGrid.tsx` 16-18、92-95、223-229
- 现状：列宽约 168px、行高 148px、图片使用 `object-cover`，不同图片比例会被裁切；用户无法切换紧凑/舒适密度。
- 建议：提供“紧凑 / 标准”两档卡片密度；图片区域使用固定比例容器，必要时提供“填充 / 完整显示”切换。不要一次引入复杂设置，先做标准模式。

### 9. 空状态文案需要按场景区分
- 位置：`src/components/AppLayout.tsx` 461-473、`src/components/ThumbnailGrid.tsx` 143-155、`src/components/TagFilterBar.tsx` 84-87、132-135
- 建议：分别区分“还没有添加根目录”“根目录尚未扫描”“当前目录没有图片”“筛选条件没有结果”“标签尚未读取”，每个场景给一个直接动作。

### 10. 预览层应提供高频动作入口
- 位置：`src/components/PreviewOverlay.tsx` 181-337
- 现状：已有翻页、缩放、拖动、标签编辑和 Ctrl+C 复制文件，但资源管理器定位、复制路径等能力不够显式。
- 建议：在顶栏增加 Tooltip 图标按钮：`在资源管理器中定位`、`复制路径`；快捷键继续保留，但不要让快捷键成为唯一入口。

### 11. 标签输入和批量操作需要处理中状态
- 位置：`src/components/ThumbnailGrid.tsx` 278-324、`src/components/PreviewOverlay.tsx` 151-179
- 现状：写标签期间按钮没有明确 loading/禁用反馈，用户连续回车或点击可能误以为没有生效。
- 建议：增加 `正在写入…` 状态，禁用输入和提交按钮；成功提示统一为“已更新 N 张图片”，失败时说明失败数量和重试方式。

### 12. 目录树操作可发现性与键盘支持不足
- 位置：`src/components/FolderTree.tsx` 303-338
- 现状：目录树使用普通 `Box` + `onClick`，没有树节点语义、Tab/方向键导航和明确的展开状态 aria 属性。
- 建议：短期补 `role="tree"`、`role="treeitem"`、`aria-expanded`、`aria-selected` 和可见 focus 样式；长期再考虑完整键盘树导航。

## P3：可选的视觉统一

- 目前同时使用 Tailwind class 和 MUI `sx`，视觉参数分散；后续应把侧栏宽度、间距、边框色、卡片间距集中成主题/token，减少局部样式漂移。
- 统一危险操作颜色和语气：`删除根目录` 是移除应用索引，不删除磁盘文件，建议按钮改为“移除根目录”，确认文案继续明确“不会删除磁盘文件”。
- “PhotoTagManager” 可增加副标题或品牌说明，例如“图片标签管理”，帮助首次打开的用户快速理解用途。
- 检查小窗口：左栏固定 `w-[280px]`，顶部按钮较多；建议设置最小窗口尺寸或在窄窗口将次要操作收进“更多”菜单。

## 推荐实施顺序

1. 上下文栏与空状态文案统一。
2. 顶部路径降级展示，避免挤压工具栏。
3. 根目录操作改为可发现的更多菜单，补键盘焦点和 aria-label。
4. 扫描/标签写入增加明确 loading、取消、失败、重试反馈。
5. 删除全局 `user-select: none`，只在图片/拖拽区域禁用选择。
6. 缩略图增加文件名、`+N` 标签提示和标准/紧凑密度。
7. 预览增加“资源管理器定位/复制路径”显式按钮。
8. 最后再做主题 token 和整体视觉精修。

## 本轮结论

没有发现必须立刻重做的视觉结构问题；现有三栏布局方向是对的。最值得先改的是“用户不知道当前看什么、操作藏得太深、状态反馈不够具体”这三类问题。