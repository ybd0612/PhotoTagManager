# PhotoTagManager UI/UX 审查概览

## 完成内容

对现有 PhotoTagManager 的文案、外观、布局、交互可用性和无障碍细节进行了只读审查，重点覆盖顶部工具栏、根目录/目录树、标签筛选、缩略图网格、全屏预览和状态反馈。

## 关键结论

- 三栏布局方向合理，暂不需要推倒重做。
- 优先优化当前上下文表达、空状态文案、扫描/写标签反馈、顶部路径占位和 hover 隐藏操作。
- `body { user-select: none; }` 范围过大，应缩小到图片拖拽等必要区域。
- 第二阶段再增加缩略图文件名、标签 `+N`、密度切换、预览层显式定位/复制路径入口和目录树 ARIA 语义。

## 交付物

- `ui-ux-review.md`：按 P1/P2/P3 分级的完整问题、源码位置、建议和实施顺序。

本轮未修改业务代码。
