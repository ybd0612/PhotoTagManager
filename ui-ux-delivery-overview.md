# PhotoTagManager UI/UX 分批修复交付概览

## 完成内容

在发布 `v0.1.1` 后，按批次完成更新机制和 UI/UX 优化：

- 更新下载防重复触发，改为后台下载，页面显示进度。
- 图片、根目录、目录树支持键盘操作和基础 ARIA 语义。
- 增加缩略图/大图加载失败、重试、跳过、扫描失败重试和批量标签提交状态。
- 优化当前目录上下文、路径展示、空状态、标签筛选文案和文本复制范围。
- 预览层增加对话框语义、焦点进入、资源管理器定位、复制路径；缩略图增加文件名和标签 `+N` 摘要。

## 验证

每个批次均通过 TypeScript 类型检查和全量测试：24/24。

## 提交记录

- `7acb32c` 更新下载改为非阻塞后台任务
- `09ef0da` 补充键盘与无障碍操作
- `4addc59` 完善状态与异常反馈
- `9a0f67c` 优化界面文案与布局
- `f090ea4` 完善预览与缩略图视觉细节

当前工作区干净，后续如需发布这些新改动，建议另行升级版本并创建新的 Release。
