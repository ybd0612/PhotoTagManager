# 照片标签管家增量功能概览

## 已完成

- 顶部目录提示改为单行显示：`目录名 · 完整绝对路径`；超长路径省略，悬停 Tooltip 可查看完整路径。
- 左上角产品名改为“照片标签管家”，同时保留英文品牌名 PhotoTagManager。
- 新增“关于照片标签管家”对话框，包含应用介绍、当前版本、GitHub 仓库、作者网站、MIT License 和学习使用说明。
- GitHub 与作者网站链接通过 Electron 主进程调用系统默认浏览器打开；主进程仅允许 HTTPS 和白名单域名 `github.com`、`yangbang.de`。

## 0.1.2 发布

- 版本号：`0.1.2`（关于页直接读取 `package.json.version`，不再硬编码）。
- 发布提交：`2958b2b 增加作者网站并通过浏览器打开外链`。
- 发布方式：推送 `v0.1.2` 标签后由 GitHub Actions 构建并创建正式 Release。

## 验证

- TypeScript 类型检查通过。
- 全量测试通过：24/24。
